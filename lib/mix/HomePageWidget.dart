import 'package:flutter/material.dart';

///首页
class HomePageWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("hello mix!"),
    );
  }
}